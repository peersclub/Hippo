{
  "name": "@hippo/admin-ui",
  "version": "0.1.0",
  "private": true,
  "description": "Operator admin panel SPA — partners, plans, users, user-wise memory, audit. Talks only to services/admin via the /api dev proxy (cookie auth, same-origin)",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5175 --strictPort",
    "build": "vite build",
    "test": "vitest run"
  },
  "dependencies": {
    "@hippo/protocol": "workspace:*",
    "@preact/signals": "^2.0.0",
    "preact": "^10.26.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vite": "^7.0.0",
    "vitest": "^3.2.0"
  }
}
