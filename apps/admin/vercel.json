{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm --filter @hippo/tokens build && pnpm --filter @hippo/ui build && pnpm --filter @hippo/protocol build && vite build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://admin-production-9c9f.up.railway.app/:path*"
    }
  ]
}
