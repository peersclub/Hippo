{
  "name": "@hippo/host-demo",
  "version": "0.1.0",
  "private": true,
  "description": "Fake partner exchange terminal — the page the SDK parasites onto during development",
  "type": "module",
  "scripts": {
    "dev": "vite --port 4000 --strictPort",
    "build": "vite build"
  },
  "devDependencies": {
    "@hippo/sdk": "workspace:*",
    "vite": "^7.0.0"
  }
}
