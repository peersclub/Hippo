{
  "name": "@hippo/portal-ui",
  "version": "0.1.0",
  "private": true,
  "description": "Partner portal SPA — a partner's team sees their own data (MAU vs quota, users), manages their integration (embed key, secret rotation) and their plan. Talks only to services/portal via the /api dev proxy (cookie auth, same-origin)",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5176 --strictPort",
    "build": "vite build",
    "test": "vitest run"
  },
  "dependencies": {
    "@hippo/protocol": "workspace:*",
    "@preact/signals": "^2.0.0",
    "preact": "^10.26.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "vite": "^7.0.0",
    "vitest": "^3.2.0"
  }
}
