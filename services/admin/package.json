{
  "name": "@hippo/admin",
  "version": "0.1.0",
  "private": true,
  "description": "Operator-facing admin service — partners, plans, users, user-wise memory management, audit. Never exposed through the partner ingress; apps/admin is its only client",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@hippo/protocol": "workspace:*",
    "@hippo/stores": "workspace:*",
    "fastify": "^5.4.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
