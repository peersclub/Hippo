# Runtime deps (BE Architecture §3: Python + FastAPI; httpx for provider +
# market-data calls). Persistence + observability (Phase 2) add:
#   redis            — optional answer-cache backend (used only when REDIS_URL
#                      is set; the in-memory cache is the default fallback)
#   opentelemetry-*  — metrics/spans for the pilot rate-card instruments
fastapi>=0.115
uvicorn>=0.30
httpx>=0.27
redis>=5.0
opentelemetry-api>=1.20
opentelemetry-sdk>=1.20
