{
  "name": "@hippo/market-data",
  "version": "0.1.0",
  "private": true,
  "description": "Normalized market snapshot API — CCXT public feeds in dev, recorded fixtures for tests and offline work",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "ccxt": "^4.4.0",
    "fastify": "^5.4.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
