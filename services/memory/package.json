{
  "name": "@hippo/memory",
  "version": "0.1.0",
  "private": true,
  "description": "Memory v1 — opt-in persona (experience level, followed assets, open threads), per-partner scoped, clearable. Regional pod; Postgres users_memory in production behind the same store surface",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "fastify": "^5.4.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
