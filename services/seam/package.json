{
  "name": "@hippo/seam",
  "version": "0.1.0",
  "private": true,
  "description": "Execution seam — Canonical Trading Interface over per-venue adapters (Build Plan/04). Approach A: Hippo prepares, the venue confirms. Sim venue adapter in dev; the Assetworks adapter is the reference implementation against a real HTTP venue.",
  "type": "module",
  "main": "./dist/lib.js",
  "types": "./dist/lib.d.ts",
  "exports": {
    ".": {
      "types": "./dist/lib.d.ts",
      "import": "./dist/lib.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "fastify": "^5.4.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
