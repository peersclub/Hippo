{
  "name": "@hippo/cli",
  "version": "0.1.0",
  "description": "hippo — the agentic installer. Understands a partner's site and API surface; generates the integration.",
  "type": "module",
  "bin": {
    "hippo": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@hippo/seam": "workspace:*",
    "commander": "^13.0.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
